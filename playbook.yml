- name: Inspiring text server setup
  hosts: all
  remote_user: root
  become: true

  tasks:
    - name: Install Aptitude via apt
      apt: name=aptitude state=latest update_cache=yes force_apt_get=yes
    
    - name: Install required system packages
      apt: name={{ item }} state=latest update_cache=yes
      loop: [ 'apt-transport-https', 'ca-certificates', 'curl', 'software-properties-common', 'python3-pip', 'python3-venv','virtualenv', 'python3-setuptools']

    - name: Add Docker GPG apt Key
      apt_key:
        url: https://download.docker.com/linux/ubuntu/gpg
        state: present

    - name: Add Docker Repository
      apt_repository:
        repo: deb https://download.docker.com/linux/ubuntu bionic stable
        state: present

    - name: Update apt and install docker-ce
      apt: update_cache=yes name=docker-ce state=latest
  
    - name: install python dependencies
      become: true
      apt:
        update_cache: yes
        state: latest
        name: python3-pip

    # - name: Install Docker module for Python
    #   ansible.builtin.pip:
    #     name: docker

    - name: Ensure Python venv is present
      ansible.builtin.command: python3 -m venv /var/www/inspiring-text-venv/
      args:
        creates: /var/www/inspiring-text-venv/

    - name: Install Docker module in venv
      ansible.builtin.command: /var/www/inspiring-text-venv/bin/pip install docker
      environment:
        PATH: "/var/www/inspiring-text-venv//bin:{{ ansible_env.PATH }}"

    - name: Copy file with owner and permissions for api
      ansible.builtin.copy:
        src: ./api
        dest: /var/www/inspiring-text/api 

    - name: Build the Docker image app
      command: cd /var/www/inspiring-text/api && ls && docker build -t inspiring-text-api . #/www/var/inspiring-text/api
    # - name: Build Docker image
    #   community.docker.docker_image:
    #     name: inspiring-text-api
    #     source: build
    #     path: /www/var/inspiring-text/api
    #     dockerfile: /www/var/inspiring-text/api/Dockerfile
    #     tag: latest

    # - name: Run the Docker container to display the public SSH key
    #   command: docker run -p 8000:8000 --rm inspiring-text-api
      # register: run_output

    # - name: Copy file with owner and permissions for app
    #   ansible.builtin.copy:
    #     src: ./api
    #     dest: /www/var/inspiring-text/api 